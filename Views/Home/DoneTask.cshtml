@{
    ViewData["Title"] = "Create New Task";
    Layout = "_Layout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/form.css" />
}
<div style="display:flex; gap:.5rem;">
    <a class="btn-back" asp-action="Dashboard">← Back</a>
</div>

<div>
    <div class="d-flex justify-content-center align-items-center" style="min-height: 85vh;">
        <div class="card p-5 text-center shadow-lg form-container">
            <h2 class="mb-5 text-uppercase form-title">Completed Task</h2>

            <form asp-action="Dashboard" asp-controller="Home" method="get">
                <!-- PRIORITY -->
                <div class="divider-form">
                    <div class="task-input-group text-start">
                        <div class="d-flex align-items-center" style="gap: 48px;">
                            <label class="task-label" style="white-space: nowrap;">Priority Level</label>
                            <div class="priority-grid">
                                <label class="badge-priority text-uppercase">
                                    <input type="radio" name="SelectedPriority" value="High" class="d-none" readonly/>
                                    High
                                </label>
                                <label class="badge-priority text-uppercase">
                                    <input type="radio" name="SelectedPriority" value="Medium" class="d-none" readonly/>
                                    Medium
                                </label>
                                <label class="badge-priority text-uppercase">
                                    <input type="radio" name="SelectedPriority" value="Low" class="d-none" readonly/>
                                    Low
                                </label>
                            </div>
                        </div>
                    </div>
                </div>
                
                <hr class="form-divider" />

                <div class="divider-form">
                    <!-- TASK NAME -->
                    <div class="task-input-group text-start">
                        <label class="task-label">Task Name</label>
                        <input type="text" class="task-form-control" placeholder="Add Title" readonly />
                    </div>

                    <!-- DEADLINE -->
                    <div class="task-input-group text-start">
                        <label class="task-label">Deadline</label>
                        <div class="d-flex align-items-center gap-2">
                            <!-- START DATE -->
                            <div class="task-input-group text-start flex-fill">
                                <input type="date" class="task-form-control" readonly />
                            </div>
                            <!-- END DATE -->
                            <span class="text-white">to</span>
                            <div class="task-input-group text-start flex-fill">
                                <input type="date" class="task-form-control" readonly />
                            </div>
                        </div>
                    </div>

                    <!-- MEMBER/S ASSIGNED -->
                    <div class="task-input-group text-start">
                        <label class="task-label">Assigned to</label>
                        <div class="member-grid">
                            <label class="badge-member mem-hanna">
                                <input type="checkbox" name="SelectedMembers" value="Johanna Angela P. Estalilla" class="d-none" readonly />
                                Johanna Angela P. Estalilla
                            </label>
                            <label class="badge-member mem-sara">
                                <input type="checkbox" name="SelectedMembers" value="Sarah Mae D.C. Harina" class="d-none" readonly />
                                Sarah Mae D.C. Harina
                            </label>
                            <label class="badge-member mem-rei">
                                <input type="checkbox" name="SelectedMembers" value="Reina Chloe D. Magpantay" class="d-none" readonly />
                                Reina Chloe D. Magpantay
                            </label>
                            <label class="badge-member mem-zach">
                                <input type="checkbox" name="SelectedMembers" value="Josiah Zachary Q. Sy" class="d-none" readonly />
                                Josiah Zachary Q. Sy
                            </label>
                        </div>
                    </div>
                </div>
                
                <div class="divider-form">
                    <!-- LINK -->
                    <div class="divider-input">
                        <div class="d-flex align-items-center gap-2">
                            <i class="bi bi-link-45deg link-icon"></i>
                            <input type="text" class="task-form-control" placeholder="Add Link (optional)" readonly />
                        </div>
                    </div>

                    <!-- DESCRIPTION -->
                    <div class="d-flex align-items-start gap-2">
                        <i class="bi bi-list editor-icon"></i>
                        <textarea class="task-form-control" placeholder="Add Description (optional)" rows="5" readonly></textarea>
                    </div>
                </div>

                <hr class="form-divider" />

                <!-- STATUS -->
                <div class="divider-form">
                    <div class="task-input-group text-start">
                        <label class="task-label">Update Task Status</label>
                        <div class="status-grid">
                            <label class="badge-status">
                                <input type="radio" name="SelectedStatus" value="Pending" class="d-none" readonly />
                                Pending
                            </label>
                            <label class="badge-status">
                                <input type="radio" name="SelectedStatus" value="In Progress" class="d-none" readonly />
                                In Progress
                            </label>
                            <label class="badge-status">
                                <input type="radio" name="SelectedStatus" value="Completed" class="d-none" checked readonly />
                                Completed
                            </label>
                        </div>
                    </div>
                </div>

            </form>

        </div>
    </div>

</div>