@model IEnumerable<TETHER.Models.TaskItem>
@{
    ViewData["Title"] = "Dashboard";
    Layout = "_Layout";

    var currentRole = ViewBag.Role as string;
    bool isPM = (currentRole == "PM");

    var assignedTasks = Model.Where(t => t.Status == "Assigned").ToList();
    var progressTasks = Model.Where(t => t.Status == "Progress").ToList();
    var approvalTasks = Model.Where(t => t.Status == "Approval").ToList();
    var doneTasks = Model.Where(t => t.Status == "Done").ToList();
}

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" asp-append-version="true" />
}

<div class="d-flex justify-content-between align-items-center mb-5">
    <h1 class="text-white m-0" style="font-size: 2.2rem; font-weight: 800;">My Tasks</h1>

    @if (isPM)
    {
        <button class="btn text-uppercase px-4 py-2 text-white" style="background-color: var(--primary-red); border-radius: 8px; font-size:0.85rem; font-weight: 700;">
            <i class="bi bi-plus-lg me-1"></i> New Task
        </button>
    }
</div>

<!-- KANBAN BOARD: TASKS -->
<div class="kanban-board">

    <!-- COLUMN 1: ASSIGNED TASKS -->
    <div class="kanban-column">
        <div class="column-header">
            <span class="column-title text-uppercase">Assigned Tasks</span>
            <span class="column-count">@assignedTasks.Count</span>
        </div>
        @foreach (var task in assignedTasks)
        {
            <div class="task-card" data-id="@task.Id">
                <div class="task-title">@task.Title</div>
                <span class="badge-priority text-uppercase">@task.Priority</span>
            </div>
        }
    </div>

    <!-- COLUMN 2: IN PROGRESS -->
    <div class="kanban-column">
        <div class="column-header">
            <span class="column-title text-uppercase">In Progress</span>
            <span class="column-count">@progressTasks.Count</span>
        </div>
        @foreach (var task in progressTasks)
        {
            <div class="task-card" data-id="@task.Id">
                <div class="task-title">@task.Title</div>
                <span class="badge-priority text-uppercase">@task.Priority</span>
            </div>
        }
    </div>

    <!-- COLUMN 3: FOR APPROVAL -->
    <div class="kanban-column">
        <div class="column-header">
            <span class="column-title text-uppercase">For Approval</span>
            <span class="column-count">@approvalTasks.Count</span>
        </div>
        @foreach (var task in approvalTasks)
        {
            <div class="task-card" data-id="@task.Id">
                <div class="task-title">@task.Title</div>
                <span class="badge-priority text-uppercase">@task.Priority</span>
            </div>
        }
    </div>

    <!-- COLUMN 4: DONE -->
    <div class="kanban-column">
        <div class="column-header">
            <span class="column-title text-uppercase">Done</span>
            <span class="column-count">@doneTasks.Count</span>
        </div>
        @foreach (var task in doneTasks)
        {
            <div class="task-card" data-id="@task.Id">
                <div class="task-title">@task.Title</div>
                <span class="badge-priority text-uppercase">@task.Priority</span>
            </div>
        }
    </div>

</div>